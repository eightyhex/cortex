---
description: Add a task to your Cortex vault
---

Use the `mcp_add_task` tool to create a task in the user's Cortex vault.

User input: $ARGUMENTS

Extract the title, description, due_date, and priority from the input if provided. Show the draft preview and ask for approval before calling `approve_draft`.
