---
description: Show Cortex vault statistics — note counts, index sizes, health
---

Use the `vault_stats` tool to get vault statistics, then use `mcp_health_check` to check system health.

$ARGUMENTS

Present a clear overview: total notes, breakdown by type, index sizes (lexical notes, semantic chunks), last rebuild time, and system health status.
