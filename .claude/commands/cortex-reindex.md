---
description: Rebuild the Cortex search index from your vault
---

Use the `rebuild_index` tool to rebuild all search indexes (lexical + semantic) from the vault.

$ARGUMENTS

Warn the user this may take a moment for large vaults. Report the number of notes indexed and timestamp when complete.
