---
description: Save a web link as a source note in Cortex
---

Use the `mcp_save_link` tool to save a link to the user's Cortex vault.

User input: $ARGUMENTS

Extract the URL, title, and description from the input. Suggest relevant tags. Show the draft preview and ask for approval before calling `approve_draft`.
