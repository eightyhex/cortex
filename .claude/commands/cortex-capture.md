---
description: Capture a quick thought to your Cortex vault
---

Use the `mcp_capture_thought` tool to capture the following thought into the user's Cortex vault:

$ARGUMENTS

If tags seem appropriate based on the content, suggest some. Show the draft preview to the user and ask for approval before calling `approve_draft`.
